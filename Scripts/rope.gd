extends Node2D
# Create a rope with 10 segments
#var rope_length = 3
#var segment_distance = 2  # Distance between each rope segment
#
#func create_rope(start_position):
	#var prev_body = null
	#for i in range(rope_length):
		#var body = RigidBody2D.new()
		#body.position = start_position + Vector2(i * segment_distance, 0)
		#add_child(body)
#
	#if prev_body:
		#var joint = PinJoint2D.new()
		#joint.body_a = prev_body
		#joint.body_b = body
		#add_child(joint)
		#prev_body = body
